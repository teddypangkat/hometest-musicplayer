package com.telkomsel.hometestmusicplayer.util

object PositionNumberList {

    fun nextPosition(currentPosition: Int) : Int = currentPosition + 1

    fun prevPosition(currentPosition: Int) : Int = currentPosition - 1

}